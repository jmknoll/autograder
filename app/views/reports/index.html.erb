<p>Input Score Here</p>

<%= form_for @report do |f| %>

	<%= f.label(:reading_score, "Reading") %>
	<%= f.number_field(:reading_score, in: 0..67) %>

	<%= f.label(:math_score, "Math") %>
	<%= f.number_field(:math_score, in: -6..54) %>

	<%= f.label(:writing_score, "Writing") %>
	<%= f.number_field(:writing_score, in: -2..49) %>

	<%= f.label(:essay_score, "Essay") %>
	<%= f.number_field(:essay_score, in: 2..12) %>

	<%= f.submit "Get Report" %>

<% end %>

<!-- 

Notes

1. Improve layout with bootstrap
2. Add form sections - make inputs display inline
3. Change scores to reflect accurately

-->